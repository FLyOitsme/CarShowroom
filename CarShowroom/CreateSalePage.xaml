<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:entities="clr-namespace:Dom;assembly=Dom"
             xmlns:local="clr-namespace:CarShowroom"
             x:Class="CarShowroom.CreateSalePage"
             Title="Создать продажу">
    
    
    <ScrollView>
        <StackLayout Padding="20" Spacing="16">
            
            <!-- Выбор автомобиля -->
            <Frame BackgroundColor="White"
                   BorderColor="{StaticResource Gray200}">
                <StackLayout Spacing="12">
                    <Label Text="Автомобиль" 
                           FontSize="18" 
                           FontAttributes="Bold"
                           TextColor="{StaticResource Black}" />
                    <Picker Title="Выберите автомобиль *"
                            ItemsSource="{Binding Cars}"
                            SelectedItem="{Binding SelectedCar}"
                            ItemDisplayBinding="{Binding DisplayText}"
                            TextColor="{StaticResource Black}"
                            TitleColor="{StaticResource Gray900}" />
                    
                    <!-- Информация об автомобиле -->
                    <Border StrokeThickness="1"
                            Stroke="{StaticResource Gray200}"
                            BackgroundColor="{StaticResource Gray100}"
                            StrokeShape="RoundRectangle 8"
                            Padding="12"
                            IsVisible="{Binding IsCarInfoVisible}"
                            Margin="0,8,0,0">
                        <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto" RowSpacing="8" ColumnSpacing="8">
                            <Label Grid.Row="0" Grid.Column="0" 
                                   Text="Марка: " 
                                   FontAttributes="Bold"
                                   FontSize="13"
                                   TextColor="{StaticResource Gray900}" />
                            <Label Grid.Row="0" Grid.Column="1"
                                   Text="{Binding CarBrand}"
                                   FontSize="13"
                                   TextColor="{StaticResource Black}" />

                            <Label Grid.Row="1" Grid.Column="0" 
                                   Text="Модель: " 
                                   FontAttributes="Bold"
                                   FontSize="13"
                                   TextColor="{StaticResource Gray900}" />
                            <Label Grid.Row="1" Grid.Column="1"
                                   Text="{Binding CarModel}"
                                   FontSize="13"
                                   TextColor="{StaticResource Black}" />

                            <Label Grid.Row="2" Grid.Column="0" 
                                   Text="Год: " 
                                   FontAttributes="Bold"
                                   FontSize="13"
                                   TextColor="{StaticResource Gray900}" />
                            <Label Grid.Row="2" Grid.Column="1"
                                   Text="{Binding CarYear}"
                                   FontSize="13"
                                   TextColor="{StaticResource Black}" />

                            <Label Grid.Row="3" Grid.Column="0" 
                                   Text="Цвет: " 
                                   FontAttributes="Bold"
                                   FontSize="13"
                                   TextColor="{StaticResource Gray900}" />
                            <Label Grid.Row="3" Grid.Column="1"
                                   Text="{Binding CarColor}"
                                   FontSize="13"
                                   TextColor="{StaticResource Black}" />

                            <Label Grid.Row="4" Grid.Column="0" 
                                   Text="Двигатель: " 
                                   FontAttributes="Bold"
                                   FontSize="13"
                                   TextColor="{StaticResource Gray900}" />
                            <Label Grid.Row="4" Grid.Column="1"
                                   Text="{Binding CarEngine}"
                                   FontSize="13"
                                   TextColor="{StaticResource Black}" />

                            <Label Grid.Row="5" Grid.Column="0" 
                                   Text="Объем: " 
                                   FontAttributes="Bold"
                                   FontSize="13"
                                   TextColor="{StaticResource Gray900}" />
                            <Label Grid.Row="5" Grid.Column="1"
                                   Text="{Binding CarEngineVolume}"
                                   FontSize="13"
                                   TextColor="{StaticResource Black}" />

                            <Label Grid.Row="6" Grid.Column="0" 
                                   Text="Мощность: " 
                                   FontAttributes="Bold"
                                   FontSize="13"
                                   TextColor="{StaticResource Gray900}" />
                            <Label Grid.Row="6" Grid.Column="1"
                                   Text="{Binding CarPower}"
                                   FontSize="13"
                                   TextColor="{StaticResource Black}" />

                            <Label Grid.Row="7" Grid.Column="0" 
                                   Text="Пробег: " 
                                   FontAttributes="Bold"
                                   FontSize="13"
                                   TextColor="{StaticResource Gray900}" />
                            <Label Grid.Row="7" Grid.Column="1"
                                   Text="{Binding CarMileage}"
                                   FontSize="13"
                                   TextColor="{StaticResource Black}" />

                            <Label Grid.Row="8" Grid.Column="0" 
                                   Text="Коробка: " 
                                   FontAttributes="Bold"
                                   FontSize="13"
                                   TextColor="{StaticResource Gray900}" />
                            <Label Grid.Row="8" Grid.Column="1"
                                   Text="{Binding CarTransmission}"
                                   FontSize="13"
                                   TextColor="{StaticResource Black}" />

                            <Label Grid.Row="9" Grid.Column="0" 
                                   Text="Тип: " 
                                   FontAttributes="Bold"
                                   FontSize="13"
                                   TextColor="{StaticResource Gray900}" />
                            <Label Grid.Row="9" Grid.Column="1"
                                   Text="{Binding CarType}"
                                   FontSize="13"
                                   TextColor="{StaticResource Black}" />

                            <Label Grid.Row="10" Grid.Column="0" 
                                   Text="Привод: " 
                                   FontAttributes="Bold"
                                   FontSize="13"
                                   TextColor="{StaticResource Gray900}" />
                            <Label Grid.Row="10" Grid.Column="1"
                                   Text="{Binding CarDrive}"
                                   FontSize="13"
                                   TextColor="{StaticResource Black}" />

                            <Label Grid.Row="11" Grid.Column="0" 
                                   Text="Состояние: " 
                                   FontAttributes="Bold"
                                   FontSize="13"
                                   TextColor="{StaticResource Gray900}" />
                            <Label Grid.Row="11" Grid.Column="1"
                                   Text="{Binding CarCondition}"
                                   FontSize="13"
                                   TextColor="{StaticResource Black}" />
                        </Grid>
                    </Border>
                </StackLayout>
            </Frame>

            <!-- Выбор менеджера -->
            <Frame BackgroundColor="White"
                   BorderColor="{StaticResource Gray200}">
                <StackLayout Spacing="12">
                    <Label Text="Менеджер" 
                           FontSize="18" 
                           FontAttributes="Bold"
                           TextColor="{StaticResource Black}" />
                    <Picker Title="Выберите менеджера *"
                            ItemsSource="{Binding Managers}"
                            SelectedItem="{Binding SelectedManager}"
                            ItemDisplayBinding="{Binding DisplayText}"
                            TextColor="{StaticResource Black}"
                            TitleColor="{StaticResource Gray900}" />
                </StackLayout>
            </Frame>

            <!-- Информация о клиенте -->
            <Frame BackgroundColor="White"
                   BorderColor="{StaticResource Gray200}">
                <StackLayout Spacing="12">
                    <Label Text="Клиент" 
                           FontSize="18" 
                           FontAttributes="Bold"
                           TextColor="{StaticResource Black}" />
                    
                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                        <SearchBar Grid.Column="0"
                                   Placeholder="Поиск клиента по ФИО..."
                                   Text="{Binding ClientSearchText}"
                                   TextChanged="OnClientSearchTextChanged"
                                   SearchButtonPressed="OnSearchButtonPressed"
                                   TextColor="{StaticResource Gray900}" />
                        <Button Grid.Column="1"
                                Text="Все"
                                Command="{Binding ToggleAllClientsCommand}"
                                BackgroundColor="{StaticResource Gray400}"
                                TextColor="White"
                                WidthRequest="60"
                                FontSize="12" />
                    </Grid>
                    
                    <Border StrokeThickness="1"
                            Stroke="{StaticResource Gray200}"
                            BackgroundColor="White"
                            StrokeShape="RoundRectangle 8"
                            Padding="8"
                            IsVisible="{Binding IsClientsVisible}">
                        <CollectionView x:Name="ClientsCollectionView"
                                        ItemsSource="{Binding FoundClients}"
                                        SelectionMode="Single"
                                        SelectedItem="{Binding SelectedClient}"
                                        SelectionChanged="OnClientSelected"
                                        HeightRequest="150"
                                        MaximumHeightRequest="200">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="entities:Client">
                                    <Border Margin="0,2"
                                            Padding="12,10"
                                            StrokeThickness="1"
                                            Stroke="{StaticResource Gray200}"
                                            BackgroundColor="White"
                                            StrokeShape="RoundRectangle 4">
                                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                                            <Label Grid.Column="0"
                                                   FontSize="14"
                                                   TextColor="{StaticResource Black}"
                                                   VerticalOptions="Center">
                                                <Label.Text>
                                                    <MultiBinding StringFormat="{}{0} {1} {2}">
                                                        <Binding Path="Surname" />
                                                        <Binding Path="Name" />
                                                        <Binding Path="Patronyc" />
                                                    </MultiBinding>
                                                </Label.Text>
                                            </Label>
                                            <Label Grid.Column="1"
                                                   Text="{Binding PhoneNumber}"
                                                   FontSize="12"
                                                   TextColor="{StaticResource Gray600}"
                                                   VerticalOptions="Center" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </Border>
                    
                    <Label Text="Или введите данные нового клиента:" 
                           FontSize="14"
                           FontAttributes="Bold"
                           TextColor="{StaticResource Gray900}"
                           Margin="0,8,0,0" />
                    
                    <Entry Placeholder="ФИО клиента *"
                           Text="{Binding ClientName}"
                           TextColor="{StaticResource Black}" />
                    
                    <Entry Placeholder="Телефон (необязательно)"
                           Keyboard="Telephone"
                           Text="{Binding ClientPhone}"
                           TextColor="{StaticResource Black}" />
                    
                    <Editor Placeholder="Паспортные данные (необязательно)"
                            HeightRequest="80"
                            Text="{Binding ClientPassData}"
                            TextColor="{StaticResource Black}" />
                </StackLayout>
            </Frame>

            <!-- Дата продажи -->
            <Frame BackgroundColor="White"
                   BorderColor="{StaticResource Gray200}">
                <StackLayout Spacing="12">
                    <Label Text="Дата продажи" 
                           FontSize="18" 
                           FontAttributes="Bold"
                           TextColor="{StaticResource Black}" />
                    <DatePicker Date="{Binding SaleDate}"
                                Format="d MMMM yyyy"
                                TextColor="{StaticResource Gray900}" />
                </StackLayout>
            </Frame>

            <!-- Цена -->
            <Frame BackgroundColor="White"
                   BorderColor="{StaticResource Gray200}">
                <StackLayout Spacing="12">
                    <Label Text="Цена" 
                           FontSize="18" 
                           FontAttributes="Bold"
                           TextColor="{StaticResource Black}" />
                    <Entry Placeholder="Цена автомобиля"
                           PlaceholderColor="{StaticResource Gray500}"
                           Keyboard="Numeric"
                           Text="{Binding Price}"
                           TextColor="{StaticResource Black}" />
                    <Label Text="{Binding FinalPrice}"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="{StaticResource AdobeAccent}"
                           Margin="0,8,0,0" />
                </StackLayout>
            </Frame>

            <!-- Дополнительные опции -->
            <Frame BackgroundColor="White"
                   BorderColor="{StaticResource Gray200}">
                <StackLayout Spacing="12">
                    <Label Text="Дополнительные опции" 
                           FontSize="18" 
                           FontAttributes="Bold"
                           TextColor="{StaticResource Black}" />
                    <Border StrokeThickness="1"
                            Stroke="{StaticResource Gray200}"
                            BackgroundColor="White"
                            StrokeShape="RoundRectangle 8"
                            Padding="8">
                        <CollectionView ItemsSource="{Binding Additions}"
                                        SelectionMode="Multiple"
                                        SelectionChanged="OnAdditionsSelectionChanged"
                                        HeightRequest="150">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="entities:Addition">
                                    <Grid Margin="0,2"
                                          Padding="12,8"
                                          ColumnDefinitions="*,Auto"
                                          ColumnSpacing="12"
                                          BackgroundColor="White">
                                        <Label Grid.Column="0" 
                                               Text="{Binding Name}" 
                                               FontSize="14"
                                               TextColor="{StaticResource Black}"
                                               VerticalOptions="Center" />
                                        <Label Grid.Column="1" 
                                               Text="{Binding Cost, StringFormat='{0:N0} ₽'}" 
                                               FontSize="14"
                                               FontAttributes="Bold"
                                               TextColor="{StaticResource AdobeAccent}"
                                               VerticalOptions="Center" />
                                    </Grid>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </Border>
                    <Label Text="Выберите опции, нажав на них" 
                           FontSize="12"
                           TextColor="{StaticResource Gray900}"
                           HorizontalOptions="Center" />
                </StackLayout>
            </Frame>

            <!-- Скидки -->
            <Frame BackgroundColor="White"
                   BorderColor="{StaticResource Gray200}">
                <StackLayout Spacing="12">
                    <Label Text="Скидки" 
                           FontSize="18" 
                           FontAttributes="Bold"
                           TextColor="{StaticResource Black}" />
                    <Border StrokeThickness="1"
                            Stroke="{StaticResource Gray200}"
                            BackgroundColor="White"
                            StrokeShape="RoundRectangle 8"
                            Padding="8">
                        <CollectionView ItemsSource="{Binding DiscountSelectionItems}"
                                        HeightRequest="150">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Grid ColumnDefinitions="Auto,*,Auto" 
                                          ColumnSpacing="8" 
                                          Padding="8">
                                        <CheckBox Grid.Column="0"
                                                  IsChecked="{Binding IsSelected}"
                                                  CheckedChanged="OnDiscountCheckboxChanged"
                                                  VerticalOptions="Center" />
                                        <Label Grid.Column="1"
                                               Text="{Binding Discount.Name}" 
                                               FontSize="14"
                                               TextColor="{StaticResource Black}"
                                               VerticalOptions="Center" />
                                        <Label Grid.Column="2" 
                                               Text="{Binding Discount.Cost, StringFormat='{0}%'}" 
                                               FontSize="14"
                                               FontAttributes="Bold"
                                               TextColor="{StaticResource AdobeAccent}"
                                               VerticalOptions="Center" />
                                    </Grid>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </Border>
                    <Label Text="Показаны только применимые скидки. Подходящая скидка применена автоматически. Можно выбрать только одну скидку или не выбирать вообще" 
                           FontSize="12"
                           TextColor="{StaticResource Gray900}"
                           HorizontalOptions="Center"
                           Margin="0,4,0,0" />
                </StackLayout>
            </Frame>

            <!-- Кнопки -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="12" Margin="0,8,0,0">
                <Button Text="Создать продажу" 
                        Command="{Binding CreateSaleCommand}"
                        BackgroundColor="{StaticResource AdobeAccent}"
                        TextColor="White"
                        FontSize="16"
                        FontAttributes="Bold"
                        HeightRequest="50" />
                
                <Button Text="Отмена" 
                        Command="{Binding CancelCommand}"
                        BackgroundColor="{StaticResource Gray400}"
                        TextColor="White"
                        FontSize="16"
                        HeightRequest="50"
                        Grid.Column="1" />
            </Grid>
        </StackLayout>
    </ScrollView>
</ContentPage>

